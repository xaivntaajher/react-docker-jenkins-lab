pipeline {

    agent any

    stages {

        stage('Build') {
            steps {
                sh 'echo "Building the application..."'
            }
        }
        
        stage('Build') {
            steps {
                sh 'echo "Building the application..."'
            }
        }


    }
}